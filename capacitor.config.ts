import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'OfflineNavigator',
  webDir: 'dist', // <-- Ubah bagian ini dari 'build' menjadi 'dist'
  
};

export default config;